# docker-compose.yml
services:
  redis:
    image: redis
    ports: ["6379:6379"]

  celery:
    build: .
    command: celery -A app.celery.celery worker --loglevel=info -Q video_tasks
    depends_on: [redis]
    volumes:
      - .:/app
